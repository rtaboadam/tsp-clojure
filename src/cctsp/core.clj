(ns cctsp.core
  (:require [cctsp.database :refer :all]
            [clojure.java.jdbc :as sql])
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
